<!DOCTYPE html>
<html>
<head>
  <script src='/js/vendor/modernizr.js'
  type='text/javascript'></script>

  <script id="version" type="text/javascript">
    // Configure require before data-main is loaded
    var require = {
      urlArgs: "version=<%= git_version_number %>",
      // Make the libraries we're using available to
      // scripts as 'jquery' instead of 'vendor/jquery'
      paths: {
        'jquery'        : 'vendor/jquery',
        'jquery-plugins': 'vendor/jquery-plugins',
        'underscore'    : 'vendor/underscore',
        'backbone'      : 'vendor/backbone'
      }
    };
  </script>
  <script data-main="/js/presenter" src='/js/vendor/require.js' type='text/javascript'></script>
  <link rel="stylesheet" type="text/css" href="/css/<%= @machine %>.css" />
</head>
<body id="<%= @machine %>">
  <%= yield %>
</body>
</html>
